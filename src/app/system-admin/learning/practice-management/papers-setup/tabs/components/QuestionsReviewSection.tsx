import React from 'react';

interface QuestionsReviewSectionProps {
  [key: string]: any;
}

export const QuestionsReviewSection: React.FC<QuestionsReviewSectionProps> = (props) => {
  // This component is now deprecated and replaced by QuestionImportReviewWorkflow
  // Keeping this as a stub to prevent import errors
  return null;
};
